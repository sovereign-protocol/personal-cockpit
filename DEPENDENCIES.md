# Dependency inventory

Runtime: `sovereign-protocol>=0.1.0,<0.2` (`LGPL-3.0-or-later`), installed as a
separately replaceable Python library. Development only: pytest `>=8,<10` (MIT)
and S-Initiative `>=0.1.0a1,<0.2` (Apache-2.0) for facade integration tests.
No source application is a S-Cockpit runtime dependency.
